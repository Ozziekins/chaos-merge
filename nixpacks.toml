[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install",
  "npm run build --workspace @chaos-merge/shared-types",
  "cd apps/api && npm run build"
]

[phases.build]
cmds = []

[start]
cmd = "cd apps/api && npm start"

